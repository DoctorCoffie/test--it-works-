Build a native executable

On macOS (64-bit):

go build -o it-works
./it-works


On Linux (64-bit):

go build -o it-works
./it-works


On Windows (64-bit, in PowerShell or CMD):

go build -o it-works.exe
it-works.exe